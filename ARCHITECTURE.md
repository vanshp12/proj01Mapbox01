# 🏗️ YatraSahayata - Architecture Overview

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                        YatraSahayata                         │
│                     Android Application                          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Presentation Layer                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    MainActivity.java                      │  │
│  │  • UI Event Handling                                      │  │
│  │  • ViewBinding Integration                                │  │
│  │  • Lifecycle Management                                   │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                  Layout Resources                         │  │
│  │  • activity_main.xml (Main UI)                           │  │
│  │  • bottom_sheet_poi_details.xml (POI Details)            │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Business Logic Layer                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Core Business Methods                        │  │
│  │  • geocodeAndFindRoute()                                 │  │
│  │  • calculateRoute()                                       │  │
│  │  • findNearbyPOIs()                                      │  │
│  │  • drawRoutes()                                          │  │
│  │  • addPOIMarker()                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      SDK Integration Layer                       │
│  ┌──────────────┬──────────────────┬─────────────────────────┐ │
│  │  Mapbox Maps │  Mapbox Search   │  Mapbox Navigation      │ │
│  │   SDK v11    │    SDK v2        │      SDK v3             │ │
│  │              │                  │                         │ │
│  │ • MapView    │ • SearchEngine   │ • MapboxNavigation      │ │
│  │ • MapboxMap  │ • Geocoding      │ • Route Calculation     │ │
│  │ • Annotations│ • POI Search     │ • Multi-route Support   │ │
│  │ • Styles     │ • Autocomplete   │ • RouterCallback        │ │
│  └──────────────┴──────────────────┴─────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      External Services                           │
│  ┌──────────────┬──────────────────┬─────────────────────────┐ │
│  │   Mapbox     │  Google Play     │   Device GPS            │ │
│  │   API        │   Services       │                         │ │
│  │              │                  │                         │ │
│  │ • Map Tiles  │ • Location       │ • Current Position      │ │
│  │ • Geocoding  │   Services       │ • Location Updates      │ │
│  │ • Routing    │ • FusedLocation  │                         │ │
│  │ • POI Data   │   Provider       │                         │ │
│  └──────────────┴──────────────────┴─────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagrams

### 1. Route Planning Flow

```
User Input (Source & Destination)
        │
        ▼
┌───────────────────┐
│ geocodeAndFindRoute()│
│ (MainActivity)     │
└───────────────────┘
        │
        ▼
┌───────────────────┐
│ SearchEngine      │
│ .search()         │
│ (Mapbox Search)   │
└───────────────────┘
        │
        ▼
┌───────────────────┐
│ Get Coordinates   │
│ (Point objects)   │
└───────────────────┘
        │
        ▼
┌───────────────────┐
│ calculateRoute()  │
│ (MainActivity)    │
└───────────────────┘
        │
        ▼
┌───────────────────┐
│ MapboxNavigation  │
│ .requestRoutes()  │
└───────────────────┘
        │
        ▼
┌───────────────────┐
│ NavigationRouter  │
│ Callback          │
└───────────────────┘
        │
        ▼
┌───────────────────┐
│ drawRoutes()      │
│ (Polylines)       │
└───────────────────┘
        │
        ▼
Display Routes on Map
```

---

### 2. POI Discovery Flow

```
User Clicks POI Button (Gas/Cafe/Hotel)
        │
        ▼
┌───────────────────┐
│ findNearbyPOIs()  │
│ (MainActivity)    │
└───────────────────┘
        │
        ▼
┌───────────────────┐
│ Get Current       │
│ Location          │
└───────────────────┘
        │
        ▼
┌───────────────────┐
│ SearchEngine      │
│ .search()         │
│ with proximity    │
└───────────────────┘
        │
        ▼
┌───────────────────┐
│ SearchCallback    │
│ .onResults()      │
└───────────────────┘
        │
        ▼
┌───────────────────┐
│ addPOIMarker()    │
│ for each result   │
└───────────────────┘
        │
        ▼
Display POI Markers on Map
        │
        ▼
User Clicks Marker
        │
        ▼
┌───────────────────┐
│ showPOIBottomSheet()│
│ (MainActivity)    │
└───────────────────┘
        │
        ▼
Display POI Details
```

---

### 3. Map Initialization Flow

```
App Launch
    │
    ▼
┌───────────────────┐
│ onCreate()        │
└───────────────────┘
    │
    ▼
┌───────────────────┐
│ Initialize        │
│ Components        │
│ • SearchEngine    │
│ • MapboxNavigation│
│ • ViewBinding     │
└───────────────────┘
    │
    ▼
┌───────────────────┐
│ Check Location    │
│ Permissions       │
└───────────────────┘
    │
    ├─── Granted ────┐
    │                │
    └─ Not Granted   │
         │           │
         ▼           │
    Request          │
    Permission       │
         │           │
         └───────────┘
                     │
                     ▼
            ┌───────────────────┐
            │ setupMap()        │
            └───────────────────┘
                     │
                     ▼
            ┌───────────────────┐
            │ Load Mapbox       │
            │ Streets Style     │
            └───────────────────┘
                     │
                     ▼
            ┌───────────────────┐
            │ Setup Annotation  │
            │ Managers          │
            └───────────────────┘
                     │
                     ▼
            ┌───────────────────┐
            │ Enable Location   │
            │ Component (Puck)  │
            └───────────────────┘
                     │
                     ▼
            ┌───────────────────┐
            │ Get Current       │
            │ Location          │
            └───────────────────┘
                     │
                     ▼
            ┌───────────────────┐
            │ Center Map        │
            │ Camera            │
            └───────────────────┘
                     │
                     ▼
              Map Ready!
```

---

## Component Interaction Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         MainActivity                             │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   MapView    │  │ SearchEngine │  │ MapboxNav    │         │
│  │              │  │              │  │              │         │
│  │ • Display    │  │ • Geocode    │  │ • Calculate  │         │
│  │ • Annotate   │  │ • POI Search │  │   Routes     │         │
│  │ • Camera     │  │ • Results    │  │ • Callbacks  │         │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘         │
│         │                 │                 │                  │
│         └─────────────────┼─────────────────┘                  │
│                           │                                     │
│  ┌────────────────────────┼────────────────────────┐          │
│  │     Annotation Managers                         │          │
│  │  ┌──────────────────┐  ┌──────────────────────┐│          │
│  │  │ PointAnnotation  │  │ PolylineAnnotation   ││          │
│  │  │ Manager          │  │ Manager              ││          │
│  │  │                  │  │                      ││          │
│  │  │ • POI Markers    │  │ • Route Lines        ││          │
│  │  │ • Route Markers  │  │ • Multi-routes       ││          │
│  │  │ • Click Events   │  │ • Styling            ││          │
│  │  └──────────────────┘  └──────────────────────┘│          │
│  └─────────────────────────────────────────────────┘          │
│                                                                 │
│  ┌─────────────────────────────────────────────────┐          │
│  │          Location Services                       │          │
│  │  • FusedLocationProviderClient                  │          │
│  │  • Current Location (Point)                     │          │
│  │  • Location Updates                             │          │
│  └─────────────────────────────────────────────────┘          │
└─────────────────────────────────────────────────────────────────┘
```

---

## Class Structure

```
MainActivity
│
├── Instance Variables
│   ├── UI Components
│   │   ├── ActivityMainBinding binding
│   │   └── MapView mapView
│   │
│   ├── Mapbox Components
│   │   ├── MapboxMap mapboxMap
│   │   ├── MapboxNavigation mapboxNavigation
│   │   └── SearchEngine searchEngine
│   │
│   ├── Annotation Managers
│   │   ├── PointAnnotationManager pointAnnotationManager
│   │   └── PolylineAnnotationManager polylineAnnotationManager
│   │
│   ├── Data Collections
│   │   ├── List<PointAnnotation> poiMarkers
│   │   └── List<PolylineAnnotation> routeLines
│   │
│   └── Location Data
│       ├── FusedLocationProviderClient fusedLocationClient
│       ├── Point currentLocation
│       ├── Point sourcePoint
│       └── Point destinationPoint
│
├── Lifecycle Methods
│   ├── onCreate()
│   ├── onStart()
│   ├── onStop()
│   ├── onDestroy()
│   └── onLowMemory()
│
├── Initialization Methods
│   ├── initializeSearchEngine()
│   ├── initializeNavigation()
│   ├── checkLocationPermissions()
│   └── onRequestPermissionsResult()
│
├── Map Setup Methods
│   ├── setupMap()
│   ├── setupAnnotationManagers()
│   ├── enableLocationComponent()
│   └── getCurrentLocationAndCenterMap()
│
├── UI Event Handlers
│   ├── setupUIListeners()
│   └── (Button click handlers)
│
├── Route Planning Methods
│   ├── geocodeAndFindRoute()
│   ├── calculateRoute()
│   ├── drawRoutes()
│   ├── addRouteMarkers()
│   └── clearRoute()
│
├── POI Methods
│   ├── findNearbyPOIs()
│   ├── getCategoryQuery()
│   ├── addPOIMarker()
│   ├── clearPOIMarkers()
│   └── showPOIBottomSheet()
│
├── Utility Methods
│   ├── formatCategory()
│   ├── createMarkerBitmap()
│   ├── createPOIMarkerBitmap()
│   └── zoomToPoints()
│
└── Constants
    ├── TAG
    └── LOCATION_PERMISSION_REQUEST_CODE
```

---

## Technology Stack Layers

```
┌─────────────────────────────────────────────────────────────┐
│                    Application Layer                         │
│  • MainActivity.java                                        │
│  • UI Layouts (XML)                                         │
│  • Resources (strings, colors)                             │
└─────────────────────────────────────────────────────────────┘
                          │
┌─────────────────────────────────────────────────────────────┐
│                    Framework Layer                           │
│  • Android SDK (API 24-34)                                  │
│  • AndroidX Libraries                                       │
│  • Material Design Components                              │
│  • ViewBinding                                              │
└─────────────────────────────────────────────────────────────┘
                          │
┌─────────────────────────────────────────────────────────────┐
│                    SDK Layer                                 │
│  • Mapbox Maps SDK v11                                      │
│  • Mapbox Search SDK v2                                     │
│  • Mapbox Navigation SDK v3                                 │
│  • Google Play Services Location                           │
└─────────────────────────────────────────────────────────────┘
                          │
┌─────────────────────────────────────────────────────────────┐
│                    Platform Layer                            │
│  • Java 11                                                  │
│  • Gradle Build System                                      │
│  • Android Runtime (ART)                                    │
└─────────────────────────────────────────────────────────────┘
```

---

## Feature Module Breakdown

```
DriveRoute Pro
│
├── Map Module (F-09)
│   ├── MapView initialization
│   ├── Style loading (Mapbox Streets)
│   ├── Location component (user puck)
│   └── Camera controls
│
├── Search Module (F-01)
│   ├── SearchEngine initialization
│   ├── Geocoding service
│   ├── Address to coordinates conversion
│   └── Search UI (TextInputLayouts)
│
├── Navigation Module (F-02, F-03)
│   ├── MapboxNavigation initialization
│   ├── Route calculation
│   ├── Multi-route alternatives
│   ├── Route visualization (polylines)
│   └── Route markers
│
├── POI Module (F-05, F-06, F-08)
│   ├── POI search by category
│   ├── Custom marker creation
│   ├── Marker click handling
│   ├── BottomSheet details
│   └── Category filtering
│
└── Location Module
    ├── Permission handling
    ├── FusedLocationProvider
    ├── Current location tracking
    └── Location updates
```

---

## State Management

```
Application State
│
├── Map State
│   ├── isMapLoaded: boolean
│   ├── currentZoom: double
│   ├── currentCenter: Point
│   └── mapStyle: Style
│
├── Location State
│   ├── hasLocationPermission: boolean
│   ├── currentLocation: Point
│   └── isLocationEnabled: boolean
│
├── Route State
│   ├── sourcePoint: Point
│   ├── destinationPoint: Point
│   ├── activeRoutes: List<NavigationRoute>
│   └── routeLines: List<PolylineAnnotation>
│
└── POI State
    ├── selectedCategory: String
    ├── poiMarkers: List<PointAnnotation>
    └── activePOIs: List<SearchResult>
```

---

## Future MVVM Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                         View Layer                           │
│  • MainActivity (UI only)                                   │
│  • Layouts (XML)                                            │
│  • Observes LiveData                                        │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                      ViewModel Layer                         │
│  • MapViewModel                                             │
│  • RouteViewModel                                           │
│  • POIViewModel                                             │
│  • Exposes LiveData                                         │
│  • Business logic                                           │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                      Repository Layer                        │
│  • MapRepository                                            │
│  • RouteRepository                                          │
│  • POIRepository                                            │
│  • Data operations                                          │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                      Data Source Layer                       │
│  • Mapbox SDKs                                              │
│  • Local Database (Room)                                    │
│  • Shared Preferences                                       │
└─────────────────────────────────────────────────────────────┘
```

---

This architecture provides a solid foundation for your final-year project while maintaining clean separation of concerns and scalability for future enhancements.
